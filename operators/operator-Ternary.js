// Ternary Operator

// If client has more than 100 points
// They are a gold client
// Otherwise
// They are a silver client

let points = 110;
let type = points > 100 ? 'gold' : 'silver';

console.log(type);