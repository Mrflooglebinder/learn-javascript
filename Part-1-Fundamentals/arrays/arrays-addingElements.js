// Arrays: Adding Elements

const numbers = [3, 4];

// add element at the end of the array
numbers.push(5, 6);

// add element at the beginning of the array
numbers.unshift(1, 2);

// add elements before a specific index
numbers.splice(2, 0, 'a', 'b');


console.log(numbers);